#!/bin/bash

# Автоматический деплой скрипт для trygo проекта
# Этот скрипт будет выполняться при получении webhook от GitHub

echo "🚀 Начинаем деплой проекта trygo..."
echo "📅 Время: $(date)"

# Переходим в директорию проекта (текущая папка)
cd "$(dirname "$0")"

# Получаем последние изменения из GitHub
echo "📥 Получаем изменения из GitHub..."
git fetch origin

# Переключаемся на main ветку и обновляем
echo "🔄 Обновляем main ветку..."
git checkout main
git pull origin main

# Проверяем статус
echo "✅ Деплой завершен!"
echo "📊 Статус репозитория:"
git status

# Показываем последние коммиты
echo "📝 Последние коммиты:"
git log --oneline -5

echo "🎉 Деплой успешно завершен!"
